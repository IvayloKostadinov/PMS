$(document).ready(function () {
    $('#meetingTheme').on('input', function () {
    });

    console.log('script.js says "I\'m here"');

    $('#meetingTheme').on('input', function () {
        var input = $(this);
        var is_meetingTheme = input.val();
        if (is_meetingTheme) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });

    $('#exampleFormControlInput12').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInput12 = input.val();
        if (is_exampleFormControlInput12) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });

    $('#exampleFormControlInput1').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInput1 = input.val();
        if (is_exampleFormControlInput1) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });

    $('#meetingType1').on('click', function () {
        console.log('script.js says "I\'m here"');
        var click = $(this);
        var select = document.getElementById("meetingType1");
        var is_meetingType1 = click.val();

        if (is_meetingType1) {
            console.log('script.js says "I\'m here2"');
            select.style.border = "3px solid green";
            // select.style("valid")
        } else {
            console.log('script.js says "I\'m here3"');
            // select.removeClass("valid").addClass("invalid");
            // select.style("invalid");
        }
    });

    $('#exampleFormControlInputProjectNumber').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInputProjectNumber = input.val();
        if (is_exampleFormControlInputProjectNumber) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#exampleFormControlInputProjectName').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInputProjectName = input.val();
        if (is_exampleFormControlInputProjectName) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });

    $('#exampleFormControlInputClientName').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInputClientName = input.val();
        if (is_exampleFormControlInputClientName) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#startDate').on('input', function () {
        var input = $(this);
        var is_startDate = input.val();
        if (is_startDate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#endDate').on('input', function () {
        var input = $(this);
        var is_endDate = input.val();
        if (is_endDate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#budget').on('input', function () {
        var input = $(this);
        var is_budget = input.val();
        if (is_budget) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });







    $('#exampleFormControlInputProjectNumberUpdate').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInputProjectNumberUpdate = input.val();
        if (is_exampleFormControlInputProjectNumberUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#exampleFormControlInputProjectNameUpdate').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInputProjectNameUpdate = input.val();
        if (is_exampleFormControlInputProjectNameUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });

    $('#exampleFormControlInputClientNameUpdate').on('input', function () {
        var input = $(this);
        var is_exampleFormControlInputClientNameUpdate = input.val();
        if (is_exampleFormControlInputClientNameUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#startDateUpdate').on('input', function () {
        var input = $(this);
        var is_startDateUpdate = input.val();
        if (is_startDateUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#endDateUpdate').on('input', function () {
        var input = $(this);
        var is_endDateUpdate = input.val();
        if (is_endDateUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#budgetUpdate').on('input', function () {
        var input = $(this);
        var is_budgetUpdate = input.val();
        if (is_budgetUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#exampleFormControlTextareaProjectDescription').on('input', function () {
        var input = $(this);
        var is_exampleFormControlTextareaProjectDescription = input.val();
        if (is_exampleFormControlTextareaProjectDescription) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#exampleFormControlTextareaProjectDescriptionUpdate').on('input', function () {
        var input = $(this);
        var is_exampleFormControlTextareaProjectDescriptionUpdate = input.val();
        if (is_exampleFormControlTextareaProjectDescriptionUpdate) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#Notes').on('input', function () {
        var input = $(this);
        var is_Notes = input.val();
        if (is_Notes) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#formControlInputNewsTitle').on('input', function () {
        var input = $(this);
        var is_formControlInputNewsTitle = input.val();
        if (is_formControlInputNewsTitle) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });



    $('#formControlTextareaNewsText').on('input', function () {
        var input = $(this);
        var is_formControlTextareaNewsText = input.val();
        if (is_formControlTextareaNewsText) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });

    $('#teamName').on('input', function () {
        var input = $(this);
        var is_teamName = input.val();
        if (is_teamName) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });


    $('#userNameInput').on('input', function () {
        var input = $(this);
        var is_userNameInput = input.val();
        if (is_userNameInput) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });





    $('#passwordInput').on('input', function () {
        var input = $(this);
        var is_passwordInput = input.val();
        if (is_passwordInput) {
            input.removeClass("invalid").addClass("valid");
        } else {
            input.removeClass("valid").addClass("invalid");
        }
    });




});